<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <!-- Core permissions for app blocking functionality -->
    <uses-permission android:name="android.permission.PACKAGE_USAGE_STATS"
        tools:ignore="ProtectedPermissions" />
    <uses-permission android:name="android.permission.SYSTEM_ALERT_WINDOW" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_SPECIAL_USE" />
    <uses-permission android:name="android.permission.RECEIVE_BOOT_COMPLETED" />
    <uses-permission android:name="android.permission.QUERY_ALL_PACKAGES"
        tools:ignore="QueryAllPackagesPermission" />
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />
    <uses-permission android:name="android.permission.VIBRATE" />
    <uses-permission android:name="android.permission.WAKE_LOCK" />
    <uses-permission android:name="android.permission.REQUEST_IGNORE_BATTERY_OPTIMIZATIONS" />

    <application
        android:name=".FocusBlockApp"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.FocusBlock"
        android:enableOnBackInvokedCallback="true"
        tools:targetApi="34">

        <!-- Main Activity -->
        <activity
            android:name=".ui.MainActivity"
            android:exported="true"
            android:theme="@style/Theme.FocusBlock"
            android:launchMode="singleTop">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- Overlay Activity for blocking apps -->
        <activity
            android:name=".ui.overlay.BlockedAppActivity"
            android:exported="false"
            android:theme="@style/Theme.FocusBlock.Overlay"
            android:launchMode="singleTask"
            android:excludeFromRecents="true"
            android:taskAffinity="com.focusblock.blocking"
            android:noHistory="true"
            android:showWhenLocked="true"
            android:turnScreenOn="true"
            android:configChanges="orientation|screenSize|screenLayout|keyboardHidden" />

        <!-- Hard Mode Unlock Activity -->
        <activity
            android:name=".ui.overlay.HardModeUnlockActivity"
            android:exported="false"
            android:theme="@style/Theme.FocusBlock.Overlay"
            android:launchMode="singleInstance"
            android:excludeFromRecents="true" />

        <!-- App Blocking Service -->
        <service
            android:name=".service.AppBlockingService"
            android:exported="false"
            android:foregroundServiceType="specialUse">
            <property
                android:name="android.app.PROPERTY_SPECIAL_USE_FGS_SUBTYPE"
                android:value="Digital wellbeing and screen time management" />
        </service>

        <!-- Accessibility Service for advanced blocking -->
        <service
            android:name=".service.FocusBlockAccessibilityService"
            android:exported="true"
            android:permission="android.permission.BIND_ACCESSIBILITY_SERVICE">
            <intent-filter>
                <action android:name="android.accessibilityservice.AccessibilityService" />
            </intent-filter>
            <meta-data
                android:name="android.accessibilityservice"
                android:resource="@xml/accessibility_service_config" />
        </service>

        <!-- Focus Cycle Overlay Service -->
        <service
            android:name=".service.FocusCycleOverlayService"
            android:exported="false" />

        <!-- Boot Receiver -->
        <receiver
            android:name=".receiver.BootReceiver"
            android:exported="true"
            android:enabled="true">
            <intent-filter>
                <action android:name="android.intent.action.BOOT_COMPLETED" />
                <action android:name="android.intent.action.QUICKBOOT_POWERON" />
            </intent-filter>
        </receiver>

        <!-- Schedule Alarm Receiver -->
        <receiver
            android:name=".receiver.ScheduleAlarmReceiver"
            android:exported="false" />

        <!-- Pomodoro Timer Receiver -->
        <receiver
            android:name=".receiver.PomodoroReceiver"
            android:exported="false" />

    </application>

</manifest>
